{
  "version": 2,
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/next" },
    { "src": "backend/api-gateway/Dockerfile", "use": "@vercel/docker" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/api-gateway" },
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
